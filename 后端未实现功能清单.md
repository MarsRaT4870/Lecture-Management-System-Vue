# 后端未实现功能清单

## 1. 评价管理模块

### 1.1 回复评价接口（前端有调用，后端未实现）
- **文件路径**: `src/views/biz/evaluation/index.vue`
- **功能点**: 管理员回复学生评价
- **接口路径**: `PUT /biz/evaluation/reply`
- **前端代码位置**: 第191-204行
- **参数**: `{ evalId, replyContent }`
- **说明**: 前端有调用 `replyEvaluation` API，但后端 `BizEvaluationController` 中没有对应的 `reply` 接口

---

## 2. 数据字段缺失问题（后端返回数据缺少前端需要的字段）

### 2.1 报名列表缺少活动名称字段
- **文件路径**: `src/views/biz/registration/index.vue`
- **使用的字段**: `item.activityName` (第32、83、270行)
- **问题**: `BizRegistration` 实体类中没有 `activityName` 字段，后端返回的报名记录缺少活动名称
- **影响**: 报名列表页面无法显示活动名称，只能显示 `activityId`
- **建议**: 后端需要在返回报名列表时关联查询活动表，填充 `activityName` 字段，或前端通过 `activityId` 额外查询

### 2.2 学分列表缺少活动名称和用户相关字段
- **文件路径**: `src/views/biz/credit/index.vue`
- **使用的字段**: 
  - `item.activityName` (第72、88、110行)
  - `item.userName` (第70、86行)
  - `item.deptName` (第87行)
  - `item.creditVal` (第89行，但实体类中是 `creditValue`)
- **问题**: 
  1. `BizCredit` 实体类中没有 `activityName`、`userName`、`deptName` 字段
  2. 字段名不匹配：前端使用 `creditVal`，后端实体是 `creditValue`
- **影响**: 学分列表无法显示活动名称、用户姓名、学院等信息
- **建议**: 后端需要在返回学分列表时关联查询活动表和用户表，填充相关字段；或前端统一使用 `creditValue`

### 2.3 活动市场页面缺少报名人数字段
- **文件路径**: `src/views/biz/market/index.vue`
- **使用的字段**: `item.regCount` (第132、274、282行)
- **问题**: `BizActivity` 实体类中可能没有 `regCount` 字段，需要关联查询报名表统计
- **影响**: 无法显示活动的当前报名人数，无法判断是否名额已满
- **说明**: 后端 `statistics` 接口中有计算 `regCount` 的逻辑（BizActivityController第241行），但列表接口可能没有

### 2.4 统计页面缺少场地数量字段
- **文件路径**: `src/views/biz/stats/index.vue`
- **使用的字段**: `stats.venueCount` (第27、100、300行)
- **问题**: 后端 `/biz/activity/statistics` 接口没有返回 `venueCount` 字段
- **说明**: 虽然 `BizStatsController` 的 `/biz/stats/dashboard` 接口有 `venueCount`，但前端使用的是 `activity/statistics` 接口
- **建议**: 在 `activity/statistics` 接口中添加 `venueCount` 字段，或使用 `stats/dashboard` 接口

---

## 3. 前端占位代码和未实现功能

### 3.1 活动详情页面路由未实现
- **文件路径**: `src/views/biz/registration/index.vue`
- **代码位置**: 第234-236行
- **问题**: `toActivityDetail` 函数中路由跳转被注释掉了
- **代码**: 
  ```javascript
  function toActivityDetail(id) {
    // router.push('/biz/activity/detail/' + id)
  }
  ```
- **说明**: 这不是虚假实现，只是功能未完成，需要创建活动详情页面

---

## 4. API接口路径问题

### 4.1 统计API路径不一致
- **前端使用的接口**: `/biz/activity/statistics` (已在 `src/api/biz/stats.js` 中修正)
- **后端提供的接口**: 
  - `/biz/activity/statistics` ✅ (BizActivityController)
  - `/biz/stats/dashboard` ✅ (BizStatsController，功能不同)
- **状态**: 已修正，前端现在使用正确的接口

---

## 5. 数据格式和类型不匹配

### 5.1 学分值字段名不一致
- **前端使用**: `creditVal` (在 `src/views/biz/credit/index.vue` 第89行)
- **后端实体**: `creditValue` (BigDecimal类型)
- **问题**: 字段名不一致可能导致数据无法正确显示
- **建议**: 统一使用 `creditValue`

---

## 总结

### ✅ 必须后端实现的接口（1个）
1. **`PUT /biz/evaluation/reply`** - 回复评价接口
   - **文件**: `src/api/biz/evaluation.js`、`src/views/biz/evaluation/index.vue`
   - **状态**: 前端已添加错误处理，后端需要实现该接口
   - **参数**: `{ evalId: Long, replyContent: String }`
   - **返回**: `AjaxResult`

### ⚠️ 建议后端优化的字段（4个问题）

1. **报名列表返回 `activityName` 字段**
   - **文件**: `src/views/biz/registration/index.vue`
   - **问题**: `BizRegistration` 实体类缺少活动名称字段
   - **影响**: 前端使用 `activityId` 作为后备显示（`活动 #${activityId}`）
   - **建议**: 后端在返回报名列表时关联查询 `biz_activity` 表，填充 `activityName`

2. **学分列表返回关联字段**
   - **文件**: `src/views/biz/credit/index.vue`
   - **问题**: `BizCredit` 实体类缺少 `activityName`、`userName`、`deptName` 字段
   - **影响**: 前端使用 `activityId` 和占位符作为后备显示
   - **建议**: 后端关联查询活动表和用户表，填充相关字段
   - **已修复**: 前端字段名 `creditVal` → `creditValue` ✅

3. **活动列表返回 `regCount` 字段**
   - **文件**: `src/views/biz/market/index.vue`
   - **问题**: 活动列表接口可能没有返回当前报名人数
   - **影响**: 无法判断名额是否已满（前端已添加 `undefined` 判断）
   - **建议**: 后端在返回活动列表时统计并填充 `regCount` 字段

4. **统计接口返回 `venueCount` 字段**
   - **文件**: `src/views/biz/stats/index.vue`
   - **问题**: `/biz/activity/statistics` 接口没有返回场地数量
   - **影响**: 统计页面场地数量显示为 0（前端已添加默认值处理）
   - **建议**: 在该接口中添加 `venueCount` 字段

### ✅ 前端已修复的问题

1. ✅ **字段名统一**：`creditVal` → `creditValue`
2. ✅ **错误处理**：所有API调用都添加了 `.catch()` 错误处理
3. ✅ **字段缺失时的后备显示**：使用 `activityId` 或占位符
4. ✅ **数据安全**：添加了空值检查和默认值（`|| []`、`|| 0`、`|| {}`）
5. ✅ **加载状态管理**：确保 `loading` 状态正确更新
6. ✅ **评价回复功能**：后端未实现时给出友好提示

### 📝 详细修改说明

请参考 `前端代码修改总结.md` 文件查看详细的代码修改说明。

